<p id="notice"><%= notice %></p>
<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>
<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<%= link_to 'Edit |', edit_user_path(@user) if current_user && @user.id == current_user.id %>
<%= link_to 'Back', users_path %>